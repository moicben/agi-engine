// Tools index for the brain module

// Import only the essential tools for now
import * as llm from '../tools/llm.js';
import * as terminal from '../tools/terminal.js';

export const tools = {
    llm,
    terminal,
};
